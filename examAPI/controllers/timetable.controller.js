import { PrismaClient } from "@prisma/client";
import  {errorHandler} from '../utils/error.js'

const prisma = new PrismaClient;
{
 /**
await prisma.signout.findUnique(...)
await prisma.signout.findMany(...)
await prisma.signout.create(...)
await prisma.signout.update(...)
await prisma.signout.delete(...)
await prisma.signout.upsert(...) 
*/ }

export const insertTimeTable = async(req, res, next)=>{

}

export const updateTimeTable = async(req, res, next)=>{

}

export const allTimeTable = async(req, res, next) =>{

}

export const deleteTimeTable = async(req, res, next)=>{

}
