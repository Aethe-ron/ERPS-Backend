import { PrismaClient } from "@prisma/client";
import { errorHandler } from "../utils/error";

const prisma = new PrismaClient;
{
 /**
await prisma.signout.findUnique(...)
await prisma.signout.findMany(...)
await prisma.signout.create(...)
await prisma.signout.update(...)
await prisma.signout.delete(...)
await prisma.signout.upsert(...) 
*/ }

export const insertEndorsement = async(req, res, next)=>{

}

export const updateEndorsement = async(req, res, next)=>{

}

export const allEndorsement = async(req, res, next) =>{

}

export const deleteEndorsement = async(req, res, next)=>{

}
